-- create alumnos

CREATE TABLE alumno (name VARCHAR(20), carrera VARCHAR(20), gender CHAR(1));

-- Add a register for alumnos

INSERT INTO alumno (name, carrera, gender) 
VALUES('Pau', 'BI', 'F');

-- select * 

SELECT * FROM alumno 

--
